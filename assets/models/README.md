# MAX wake-word model

Place the trained TensorFlow Lite wake-word model here as:

`assets/models/hey_max_model.tflite`

The Android wake engine loads that asset at runtime. The model is intentionally not included until the MAX voice training dataset and training pipeline are complete.
